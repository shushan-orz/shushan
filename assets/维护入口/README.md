# 维护入口

这个文件夹就是作品网页的主要维护入口。

- `地编`：每个 `project-xx` 是一个地编项目。
- `模型`：直接放模型展示图片，图片文件名会作为作品名。
- `蓝图`：直接放 UE 复制出来的 `.txt` 蓝图文件，文件名会作为蓝图按钮名。

增删文件后，回到网页根目录运行 `刷新维护入口.ps1`，页面会读取新生成的 `portfolio-manifest.js`。
