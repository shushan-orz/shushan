# 拖尾维护入口

- `config.txt` 用于控制拖尾。
- `enabled=1` 开启拖尾，`enabled=0` 关闭拖尾。
- `scale` 控制图片拖尾缩放，例如 `0.8`、`1`、`1.5`。
- `opacity` 控制图片拖尾透明度，范围建议 `0.05` 到 `1`。
- 这个文件夹里放入图片时，拖尾会优先使用第一张图片。
- 没有图片时，网页会自动使用默认的圆润线条拖尾。
